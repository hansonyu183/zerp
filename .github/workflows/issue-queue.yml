name: Issue delivery queue

on:
  repository_dispatch:
    types: [issue-queue]
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: issue-delivery-queue
  cancel-in-progress: false

jobs:
  dispatch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/create-github-app-token@fee1f7d63c2ff003460e3d139729b119787bc349 # v2
        id: implementer_token
        with:
          app-id: ${{ vars.ZERP_IMPLEMENTER_APP_ID }}
          private-key: ${{ secrets.ZERP_IMPLEMENTER_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: ${{ github.event.repository.name }}
      - name: Dispatch eligible work
        env:
          GH_TOKEN: ${{ steps.implementer_token.outputs.token }}
          ZERP_AUTOMATION_ENABLED: ${{ vars.ZERP_AUTOMATION_ENABLED }}
          ZERP_IMPLEMENTATION_CAPACITY: '2'
        run: scripts/issue-queue.sh
