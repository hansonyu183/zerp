# ZERP Domain Language

ZERP uses shared business terms across its auxiliary-data, business-object, voucher, workflow, and business-ledger domains. This glossary fixes the meaning of terms that cross those domain boundaries.

## Authorization

**Delegation Ceiling（授权上限）**:
管理员当前最终权限集合形成其授权上限；管理员只能创建、修改或分配有效权限不超过该上限的角色。`superadmin` 的未来通配语义也属于授权范围，因此只能由当前 `superadmin` 分配。
_Avoid_: 角色管理权限等于全部权限、可授予未拥有权限

## Business Objects

**Customer Account（客户结算子账户）**:
与本企业发生具体销售和独立结算关系的客户账户。销售订单、账期、应收、信用额度和开票跟踪都归属子账户；每个子账户必须且只能属于一个集团客户。
_Avoid_: 用共享付款公司合并多个客户的账期和应收、共享业务伙伴角色

**Customer Group（集团客户）**:
归集一个或多个客户结算子账户的收款识别根，拥有共享公司名称、税号、开票资料和付款银行账户。集团是带 revision 和审计的当前资料，不走子账户的版本、提交和审核状态机；普通客户同样使用集团模型，只包含一个子账户。
_Avoid_: 集团统一应收或信用额度、集团规则由子账户继承覆盖、只在多客户特例时临时建组

**Operating Entity（经营主体）**:
我方实际承担合同销售方、开票方和收款方责任的法人公司。销售订单保存最终经营主体快照，发票、应收、来款和资金账户必须保持同一经营主体边界。
_Avoid_: 商品品牌、客户类型、报表标签、允许跨经营主体收款分摊

**Sales Receipt Allocation（销售收款分摊）**:
把一笔集团客户来款在同一经营主体边界内明确分配到一个或多个客户结算子账户及其未结应收的金额明细。系统只对该经营主体的应收跨子账户按已逾期优先、到期日、单据日期和单号稳定生成建议，再处理未到期应收；单子账户集团自动分配，多子账户集团由人工确认或调整，全部金额分摊完成才能批准。
_Avoid_: 依付款公司直接冲减共享余额、把一笔银行流水伪造为多笔来款

**Invoicing Requirement（开票义务）**:
由销售签收批准时保存的集团税号快照直接派生：税号非空时形成该子账户待开票金额，税号为空时进入未开票收入申报；销售退货按原签收归属冲减。集团税号后续变化只影响之后确认的收入。
_Avoid_: 是否开票布尔值、由每张订单任意选择是否需要开票、按集团当前税号重分类历史

**Supplier（供应商）**:
与本企业发生采购或物流服务关系的独立 BOB 业务对象。相同税号的客户集团或其他往来单位不与其共享身份、生命周期或编码。
_Avoid_: 共享业务伙伴下的供应商角色

**Other Dealings Unit（其他往来单位）**:
承担销售、采购之外其他债权债务关系的独立 BOB 业务对象。
_Avoid_: 共享业务伙伴下的其他往来角色

**Customer Type（客户类型）**:
客户的可配置业务分类，初始分类为终端客户和经销商。销售订单保存类型快照，居间和业绩脚本可以读取其编码；当前自动定价算法不读取客户类型，字典项自身也不保存计算规则。
_Avoid_: 宣称客户类型当前决定售价、把价格和业绩公式塞进字典项、固定写死两个类型

**Settlement Method Snapshot（结算方式快照）**:
客户版本从 AUX 结算方式复制并直接拥有的结算事实，包括来源 ID、编码、名称、术语、到期计算参数和销售加价，不依赖来源版本继续存在或保持启用。
_Avoid_: 制单时逐层解析结算方式版本、只保存结算方式 ID

**Settlement Timing（结算时间规则）**:
预付、现结、货到若干天、当月结或月结若干天等“何时应付款”的规则，由客户结算方式快照表达。
_Avoid_: 把银承、电汇、现金等付款媒介当作月结规则

**Payment Method Snapshot（收款方式快照）**:
客户默认使用的付款媒介及其销售价格影响。客户版本从 AUX 收款方式复制来源 ID、编码、名称和默认销售加价；新销售订单可以改选并保存最终快照。
_Avoid_: 承兑类型、`cd_type`、把收款方式合并进结算时间规则

**Customer Transport Policy（客户运输政策）**:
客户版本保存默认运输方式和客户约定运输加价。新销售订单默认带入，允许按单修改，并保存最终运输方式和加价快照。
_Avoid_: 从当前客户资料回算历史订单、把运输方式和运输价格混成一个字段

**Sales Cost Component（销售成本组成）**:
为了履行该笔销售而发生、必须从成交价中覆盖的具名成本，例如结算时间、收款方式、运输、制造或第三方居间成本。同一订单可以有多项不同名称的成本组成；计算口径只分为按计价数量计算的单位型成本和每张订单固定发生的整单型成本。
_Avoid_: 其他加价、无法说明来源或去向的成本

**Sales Premium（销售溢价）**:
在基础报价和全部销售成本组成之上额外加入成交价的非负单位金额。客户以独立顶层字段保存默认溢价单价，订单保存实际采用值。
_Avoid_: 额外利润、与成本或优惠混记

**Sales Discount（销售优惠）**:
从自动价格中向下扣减的非负单位价格。客户保存默认优惠单价，新订单自动带入；该值同时就是客户免批优惠单价，订单最终优惠单价超过它才触发特殊批准，审批不比较整单优惠总额。
_Avoid_: 负溢价、用有符号价格字段同时表达利润和优惠

**Customer Sales Attribution（客户业务归属）**:
每个客户结算子账户同一时间唯一的主要业务关系，类型为本公司专职业务员、外部兼职业务员或经销型业务关系。销售订单保存该归属快照；第三方居间金额计入该业务归属名下，并继续按客户结算子账户区分。
_Avoid_: 同一客户同时配置多个主要业务归属、把不具名第三方居间另建为客户资料中的具名收款方

**Third-party Intermediary Cost（第三方居间成本）**:
为真正的、不具名第三方居间预留的销售成本。固定部分按 kg 计算，对应 OIT/KY 的 `hk_price`；浮动部分从业务差价中按规则分取，对应 `fd_price`。两项可以同时存在，均不属于客户返点、员工提成或客户优惠。
_Avoid_: 要求客户资料绑定具名收款方、把 `fd_price` 解释为客户返点、把员工销售提成统称为第三方居间费

**Customer Credit Limit（客户信用额度）**:
客户结算子账户在单一交易币种内获批的最大信用占用。ACC 按子账户提供实时已占额度，销售订单超过额度时只能由具有专门权限的人员填写原因后批准；集团客户不另设合计额度。
_Avoid_: 按业务员或集团汇总客户额度、只提醒不控制、跨币种直接相加

**Customer Internal Reminder（客户内部提醒）**:
选择客户时向有权用户展示、但不复制到销售单据的内部业务提示。
_Avoid_: 客户备注、自动进入对外单据

**Default Sales Order Remark（默认销售订单备注）**:
从客户版本复制到新销售订单、随后属于订单且允许制单人编辑的默认备注。
_Avoid_: 客户内部提醒、订单保存后继续回查客户当前值

**Transferable BOB Reference（可转移 BOB 关联）**:
当前有效 BOB 版本中直接引用另一个当前 BOB 对象的业务字段。被引用对象失效前，系统统一扫描全部此类字段并把当前引用批量改派到同类型接任对象；AUX 引用、审计身份、历史 BOB 版本和 VOU 单据快照不是可转移关联。
_Avoid_: 只处理客户业务员、按页面硬编码关联白名单、改写历史操作者或交易快照

## Navigation

**APP Page Route（APP 页面路由）**:
APP 领域管理页面在菜单目录和前端路由中使用的稳定 `routeKey` 与 `routePath`。用户、角色、权限、系统参数和菜单均属于 APP 领域；“管理”是职责描述，不是 `admin` 领域或路由命名空间。
_Avoid_: admin 领域、`admin/*` 管理页面路由、把页面路由等同于 API 动作路径

## Configuration

**Configured Value（配置值）**:
已登记系统参数当前持久化的目标值。保存或恢复默认值只更新配置值；其是否已被运行实例采用由生效模式决定。
_Avoid_: 当前值、已生效值

**Running Value（运行值）**:
运行实例已经采用的系统参数值。仅当部署范围内全部运行实例已被证明采用同一最新配置版本时，才更新为配置值。
_Avoid_: 推测已生效的配置值

**Effect Mode（生效模式）**:
系统参数值从配置值影响运行行为的既定方式；包括立即生效、下次请求生效和重启后生效。
_Avoid_: 页面重启、未说明的生效时机

## Accounting

**Accounting Subject（会计科目）**:
归 ACC 领域和单本会计账簿所有的分层会计分类。不同账簿的科目相互独立，AUX 不维护全局会计科目或收支类型到科目的直接映射。
_Avoid_: AUX 会计科目、全局会计科目

## Voucher Lifecycle

**Voucher Posting（单据入账）**:
会产生业务台账流水的 VOU 单据进入 `APPROVED` 时，将业务事实写入库存、资金、往来、空桶等 LED 台账；`unapprove` 撤销同一批台账流水。没有台账流水的单据仍可进入 `APPROVED`。
_Avoid_: 最终处理、业务完成

## Other Dealings

**Other Dealings Ledger（其他往来台账）**:
记录销售、采购往来之外的其他债权与债务；余额按主体等既有账簿维度计算，类别只作为流水属性和筛选条件。
_Avoid_: 其他单位台账、员工借款台账

**Other Dealings Subject（其他往来主体）**:
其他往来的权利义务对象，可以是客户、供应商、其他单位或员工；主体身份不因进入其他往来台账而被复制或改变。
_Avoid_: 其他往来单位

**Other Dealings Category（其他往来类别）**:
其他往来流水的可选业务分类，例如员工销售提成、外部销售服务费或第三方居间；类别不参与余额维度，工资也不属于其他往来类别。
_Avoid_: 单据类型、主体类型
