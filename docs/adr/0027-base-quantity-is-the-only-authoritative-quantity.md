# Base quantity is the only authoritative quantity

ZERP 为每个产品定义不可管理且不落为单位对象的抽象基准尺度，并把无单位基准数量作为配方、履约、库存和成本的唯一权威数量。业务单据必须另存录入数量和单位用于展示与审计，但产品单位换算只帮助前端得到建议基准数量，不保存换算系数，也不进入后端业务规则或库存台账；已批准基准数量错误时必须反审原单重做，不重算历史，也不用调整单掩盖原错误。
