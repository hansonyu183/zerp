# #413 ACC、RPT 与 WFL 动态页面迁移

后续状态：居间计算已在[动态页面复核修复验收](dynamic-pages-review-fixes-2026-09-09.md)补齐，人工录入覆盖达到 32/32。本文保留原阶段的实现范围、缺口与实际退出码，不作为最新完成状态。

## 范围与状态

基线为 `f0fdb196`。用户明确允许先实施 #413 的 ACC/RPT/WFL 部分，保留 #412 依赖缺口；本记录不宣布 #413 或父规格 #408 完整关闭。实现 SHA：`e7e36b416874b79112d3458612142acdceb06a76`，在既有 `codex/issue-412-voucher-entry` 分支本地提交；本记录随后单独提交。

全部现有已登记业务资源进入六类动态页面。人工制单仍为 **31/32 类**：本次没有新增居间计算录入。先前将现行公式或新版全局脚本未确认判断为开发阻碍有误；用户随后明确居间计算采用持久化动态脚本，公式调整不构成开发前置条件。尚未交付的是来源生成、全局脚本维护、页面计算及其验证链路，属于实现工作。此更正不改变下述已执行测试的覆盖范围。未执行生产部署、业务数据迁移、推送、PR 或合并。

## 当前页面能力

| 页面类型 | 当前资源                       | 页面责任与局部区块                                                                           |
| -------- | ------------------------------ | -------------------------------------------------------------------------------------------- |
| 直接维护 | APP 用户/角色，已登记 AUX 资料 | 查询、临时编辑、精确动作、启停与刷新；沿用既有直接维护 definition                            |
| 版本档案 | BOB 五类档案、WFL 流程定义     | 当前/候选/历史/审计、附件、局部定价/配方/脚本试算                                            |
| 单据     | 36 类普通 VOU 与独立会计期初   | 不可变 Submission、临时 Draft、审批；31 类人工录入，4 类系统生成不开放新建，居间计算仍待交付 |
| 配置     | ACC mapping                    | 当前 revision、保存及领域校验、未知结果核实；映射区块只编辑配置值并复用 FormBlock            |
| 报表     | 受约束 `rpt-XXXXXX`            | 原生参数、独立引用请求、参数快照、page/hasMore、列展示与 CSV；查询/导出权限独立              |
| 流程运行 | WFL process-instance           | 实例、节点意图、原因、执行结果、审计与 VOU 正文读取；NodesBlock 不持有网络或整页状态         |

ACC 现有协议只有 catalog/query/get/save，校验由保存时 Domain Service 完成，没有独立映射试算接口。本次不新增试算协议或通用规则语言。

## 实际验证

本任务独占 `compose.target.yaml` 项目 `zerp-issue-413`，PostgreSQL/API/Web 端口为 `55444/18092/18093`。运行命令由仓库内临时包装 `.scratch/issue-413/run.sh` 设置；日志仅在 `.scratch/issue-413/`，不提交测试账号或数据库连接串。

旧 WFL、目录和期初浏览器运行器使用内嵌 API/Vite，不能证明 Compose 的真实消费者。本片删除这些服务装配，统一要求显式 loopback、isolated、`*_test` 拓扑；Makefile 在各浏览器专项前重建本任务独占库，再启用同一 Compose API/Web。夹具通过领域服务持久化，脚本 `finally` 关闭连接，整套数据库由任务回收；WFL 不再使用会计空协作。

| 命令                                            | 退出码 | 结果                                                                                                                              |
| ----------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------- |
| `sh .scratch/issue-413/run.sh make check e2e`   | 2      | 生成、完整检查、模型 33、API 单元 82 / 工件 19 / 集成 108、迁移及通用浏览器 28 项通过；WFL 打开后审计刷新断言失败，未执行后续专项 |
| `pnpm --dir frontend check`                     | 0      | 最后前端候选的类型、lint、格式、构建、233 项单元及 10 项真实 Vuetify 测试                                                         |
| `pnpm --dir frontend typecheck`                 | 0      | 额外 definition 错误组合类型约束                                                                                                  |
| `pnpm docs:check`                               | 0      | 权威文档与用例覆盖检查                                                                                                            |
| `sh .scratch/issue-413/remaining.sh`            | 1      | 最终 Web 重建；WFL 1 项通过，目录响应等待条件失败后停止                                                                           |
| `sh .scratch/issue-413/rest.sh`                 | 0      | 同一最终 Web；目录 2 项、期初 2 项、录入 58 项通过，全部使用独占 Compose                                                          |
| `make check-common check-ci-workflow`           | 0      | 最终公共、文档、格式及 CI 工作流检查                                                                                              |
| 独占 `make target-down`、任务镜像删除及资源回读 | 0      | 仅回收本任务容器/卷/网络及两个命名镜像；无剩余独占端口                                                                            |

WFL 审计修复后重新构建 API/Web，按 Makefile 的后续步骤执行专项：`sh .scratch/issue-413/remaining.sh` 首先完成 WFL 1 项（退出 0），随后目录测试把首次加载响应误认为筛选响应而失败；已将等待条件限定到请求携带的目标单号。`sh .scratch/issue-413/rest.sh` 重建独占库后逐项执行目录、期初与录入，目录 2 项和期初 2 项均退出 0，录入 58 项（29 类 × 两种宽度）全部退出 0，整个续跑脚本退出 0。前面已通过且未受后续 WFL 页面/测试修复影响的生成、后端、通用浏览器证据继续覆盖候选，不将首次聚合退出码改写为成功。

公开 seam 使用真实 definition → Registry/ResourceHost → 用户输入/动作 → 类型化 API；不导出私有 runtime refs。配置页回归覆盖 revision 冲突、保留无效输入、取消零刷新、保存一次与刷新失败、未知锁跨对话框保留、精确内容/revision 核实后刷新一次、共享中文字段。报表回归覆盖日期参数快照、仅导出权限、资源和 Session 切换、迟到下载阻断及两个引用源逆序响应。流程运行回归覆盖动作与 VOU 精确权限、当前请求审计核实、内部错误锁定、正文读取失败、写成功但列表/审计刷新失败、会话隔离及查询乱序。

## DP-01–12 核对

| ID    | 本轮证据及边界                                                                                                                            |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| DP-01 | 实际 Registry 穷尽检查六类运行时及全部 VOU 目录；生产业务注册不再导入 pages 路径                                                          |
| DP-02 | 三个新 definition 只有身份与 kind；类型错误测试拒绝 VM、任意 query 回调及错误资源组合；Host 只传 definition                               |
| DP-03 | ACC 规则/分录复用公共表单；映射和节点区块不拥有提交、审批、网络写入或整页 VM                                                              |
| DP-04 | 通用浏览器 28 项包含已登记 APP/AUX 当前消费者、精确权限及桌面/390px 操作                                                                  |
| DP-05 | 通用浏览器覆盖五类 BOB，WFL 专项 1 项通过定义试算、提交批准、换版及旧实例                                                                 |
| DP-06 | **未完成：31/32，居间计算缺失**；不以 29 类专项加两类订单替代全量完成                                                                     |
| DP-07 | 四类系统生成无人工创建、期初独立载荷；当前真实 Compose 目录 2 项、期初 2 项均通过                                                         |
| DP-08 | 当前公开测试覆盖精确动作、VOU 正文权限和并行引用；领域侧真实 HTTP/PostgreSQL 权限和 blocker 回归                                          |
| DP-09 | 本轮公开测试覆盖未知锁、核实、一次刷新、失败不重放及 Session/资源/查询迟到隔离                                                            |
| DP-10 | ACC 映射与 RPT 两种权限场景在通用 Compose 浏览器通过；WFL 真实 Compose 专项 1 项通过                                                      |
| DP-11 | ACC 在桌面/390px 保存，RPT 查询分页和 CSV 保持精度及空值，WFL 390px 节点操作及正文读取通过；三页均无页面横向溢出                          |
| DP-12 | 旧页面/VM/专属测试删除，ADR 双向部分替代关系保留，Agent 约束更新；门禁全部步骤完成分阶段复验，独占资源已回收；首次聚合退出 2 保留如实记录 |

## 独立审查

Standards 初轮发现未知保存关闭后丢失可见输入、核实成功未刷新列表，两项均先红后绿修复。Spec 初轮发现 WFL 审计刷新错误被列表读取清除，已分离错误并补公开回归。两路定点复核未发现未解决项。浏览器新增断言随后发现打开单据后没有刷新审计，已通过公开测试先红后绿修复，读取正文前再次检查 Session/实例 generation，独立定点复核通过。补迁旧查询行为测试另发现 WFL 较早响应提前解除最新查询加载状态，已先红后绿修复。

## 清理与交付

`make target-down` 已显式回收本任务 API/Web/PostgreSQL 容器、网络和附件卷；两个本任务镜像 `zerp-issue-413-target-api:latest`、`zerp-issue-413-target-web:latest` 已删除。回读项目容器、网络、卷及镜像为空，55444/18092/18093 无监听；本仓库专项运行器及 Playwright 已退出。共享 `zerp-back` 服务和另一仓库的浏览器进程未操作。历史 #409–#412 记录仅说明其交付范围，不替代本候选执行结果；居间计算未交付，整批最终验收保持未完成。
