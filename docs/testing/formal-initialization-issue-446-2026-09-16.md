# #446 正式初始化准备与隔离验证（2026-09-16）

关联规格：[Issue #446](https://github.com/hansonyu183/zerp/issues/446)。本记录区分实现、演练与正式执行；不代表鑫泉整仓迁移完成。

## 已核验的唯一目标

| 项目                | 2026-09-16 只读核验结果                                                |
| ------------------- | ---------------------------------------------------------------------- |
| Docker 实例         | 当前 Mac 的 `colima` context                                           |
| Compose 项目        | `zerp-back`                                                            |
| 配置                | 本仓库 `compose.yaml`、`compose.production.yaml`，受控本地生产环境文件 |
| 容器                | `zerp-back-db-1`、`zerp-back-api-1`、`zerp-back-web-1`                 |
| 数据库 / 数据库用户 | `zerp` / `zerp`                                                        |
| 数据库存储          | `zerp-back_postgres-data`，数据库内只有 `public` 应用 schema           |
| 运行版本            | API/Web 均为 `2f033b1644e9094d61d56b077bf407c9e1eb106b`                |
| 公网入口            | `https://zerp.bytesucceed.com`、`https://zerp-api.bytesucceed.com`     |
| 只读基线            | 两个测试用户、3 个档案主体、15 张单据、13 个 Session 记录              |

容器 release 标签、公网 Web release marker 及 API 健康响应一致。此实例不是 `al-sz-root` 上的其他服务，也不是本次 `zerp-target` 或初始化演练库。未读取、复制或备份业务数据正文。

## 实现范围

- 用 `initialize:admin` 与 APP 服务替换固定内测用户种子；生产启动依赖正式初始化完成。
- 首次只建立配置的管理员及保留 `superadmin` 关联，要求首次改密；已有账号完全由正常 APP 流程管理，重启不覆盖密码、名称、状态或授权。
- 删除旧种子脚本、服务方法、类型和专属测试；没有保留运行时兼容入口。
- 正式账号编码、显示名称与受控凭证文件由部署明确配置；没有默认指定身份。
- 数据库外附件未修改；生产数据库未清空。

## 验证

- TDD：新增初始化 CLI 测试首先因命令不存在而失败，实现后通过。
- 独占随机 `_test` 数据库验证并发初始化只创建一人、首次改密、旧会话失效、重复启动保留密码和会话、移交超级权限后不恢复授权、非空基线拒绝新增管理员、清库重建后的旧身份失效及空业务表；结束后删除演练库和临时凭证。
- Standards 和 Spec 独立审查均为零代码发现。正式执行等待管理员身份是规格内前置条件，不能计为完成。

生产 Compose 演练使用独占 `zerp-initialization-446` 项目及随机凭证，在 `compose.yaml + compose.production.yaml` 上启动数据库、目录同步、正式初始化、API 和 Web；仅为本地 HTTP 验证覆盖 Cookie Secure 设置。健康检查、真实浏览器首次登录与强制改密、新密码登录、完整启动链重建、旧测试账号拒绝登录、新会话跨重启保留全部通过。回读为一个用户、一个角色、零逐项超级管理员授权，档案、单据和账簿为空。容器、网络和数据卷已删除。

完整 `make e2e` 退出码为 0：生成物、公共/CI 检查、静态检查、单元/组件、WFL Node/browser parity、119 个 PostgreSQL 集成测试、Compose 构建与健康检查均通过；五组浏览器分别通过 32、1、6、2、66 个场景，共 107 个。运行后另行通过公共文档/格式检查和 21 个生成工具测试。初始化演练镜像及临时凭证已删除，`zerp-target` 验收服务按会话所有权回收。

## 正式执行前置条件

正式管理员的账号编码、显示名称与初始凭证来源尚待用户明确；现有部署只能识别 `test-admin` 和 `tester`，不能据此推定正式身份。#446 已明确不备份当前 ZERP 数据，此决定无需再次确认。

当前没有停止生产业务写入，没有执行清库或发布。管理员明确后须按[正式初始化运行手册](../operations/formal-initialization.md)完成发布、清空、初始化、真实登录、重复启动及独立回读，之后才能完成 #446。附件存储不在本次删除范围。
