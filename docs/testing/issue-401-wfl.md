# #401 WFL 定义归属与版本验证

2026-09-07，直接使用授权的 `zerp` 数据库完成分项验证。未执行重建数据库的聚合命令，未恢复 CI 或部署。

- 执行 `migrate:wfl`，完成 WFL 稳定标识、审批归属及精确权限的一次性转换；本次实际待转换数据计数均为 0。从可执行路由和转换后的数据库生成 OpenAPI、权限目录及 Kysely 类型。
- API、api-client、frontend 类型检查，前端 lint 和构建通过。
- API 单元 79 项、生成及 schema 检查 19 项、前端单元与真实组件测试 206 项通过。
- 直接运行 `wfl-core`、`wfl-ownership`、`wfl-migration`、`workbench` 集成测试，7 项通过；`acc-wfl-http` 的 WFL 认证 HTTP 测试 1 项通过。
- 数据库用例覆盖提交幂等与冲突、审计失败原子回滚、不可变版本、独立运行开关、旧实例固定版本、重复审批不生成第二实例、六种节点动作，以及有历史数据与授权的迁移和迁移失败回滚。测试通过外层事务回滚清除样例数据。
- 真实 Playwright `wfl.spec.ts` 1 项通过：编辑与试跑、提交、审核、启用、真实 VOU 生成实例、第二版本审核与停用、旧实例继续生成子单、刷新销毁临时输入。
- 文档检查 8 项通过，格式检查与 `git diff --check` 通过。

独立 Standards、Spec 审查发现的问题均已修复并复核，无剩余发现。本次浏览器验证启动的临时 API、Vite 与浏览器已关闭，数据库测试事务已回滚；共享数据库服务保留。
