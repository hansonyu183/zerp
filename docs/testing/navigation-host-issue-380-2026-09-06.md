# 2026-09-06 权限导航与 Host 验收（#380）

本记录对应 `codex/378` 集成分支的第二片 #380，基于已提交的 #379 Session 契约。它不代表 #381 用户管理、全域迁移、合并或生产部署已经完成。

## 实际交付

运行链路为 Session `apiPaths` → `resourceGroups` → AppLayout 分组导航 → `/:domain/:entity` → ResourceHost → Registry。本票 Registry 为空；已授权资源明确显示未实现，不挂载任何业务页、不查询、不回退原页面。列表 Shell、用户页业务 VM 和用户回调由 #381 接入。

同资源动作去重，Session 不入菜单；资源入口不要求 query、不与页面登记求交集。菜单、直达和权限刷新共用资源判定；Session 代次与资源键控制组件实例，资源撤权、受限会话、退出和账号切换使业务实例失效。

静态生成目录包含 771 条精确权限、87 个资源。已知资源使用中文名称，动态或缺少展示映射的资源保留标识与待配置说明。

| 域  | 静态资源数 | 本票页面状态                           |
| --- | ---------- | -------------------------------------- |
| APP | 4          | 全部未实现，包含待 #381 接入的用户管理 |
| AUX | 13         | 全部未实现                             |
| BOB | 11         | 全部未实现                             |
| DCL | 13         | 全部未实现，既有服务与归属保留         |
| VOU | 38         | 全部未实现                             |
| ACC | 5          | 全部未实现                             |
| RPT | 1          | 目录未实现，动态资源按当前权限另行出现 |
| WFL | 2          | 全部未实现                             |

`app/workbench/query` 保持既有会话范围聚合接口及按 DCL/VOU 权限计算结果的规则，不凭空新增独立权限或菜单特判。DCL 的归属冲突、ACC 期初和配置资料迁移仍按 ADR-0052 单列为后续工作。

## 复用与清理

- 复用 AppLayout 的侧栏、顶栏、原有分组图标、288px 抽屉、响应式行为、个人资料/改密弹窗，以及 zerpLight/zerpDark 主题和 AppSnackbar。
- 从原用户页提取 `ManagementPageFrame`，Host 实际使用其卡片与布局。原页没有独立展示组件，因此另提取 `UserListPresentation` 和 `UserEditorPresentation`，保留表格、分页、反馈和表单弹窗供 #381 复用；两者只有 props/emits，没有 API、Session、业务 VM 或页面登记。
- 移除菜单读取、保存、模式切换、恢复四个接口及三条菜单管理权限；生成契约差异核对未删除其他业务路径。删除模板表、权限展示列、启动同步、编辑页及专属测试。
- 按 Router、Registry 和导入引用删除不可达业务页面、VM、本地草稿/编辑辅助代码及专属测试和页面用例，保留存续业务的类型化 API 适配与真实服务测试。前端专属 PostgreSQL 测试依赖也随相应浏览器页测试移除。
- 新增显式运行的[菜单结构清理流程](https://github.com/hansonyu183/zerp/blob/c9c2d8fca30d9da7b4046315733b7666d564b0d5/docs/operations/menu-structure-cleanup.md)。真实隔离库验证非空结构删除后用户、角色、其他授权和审计不变，重复执行幂等，不完整基线拒绝执行。

## 验证范围

- 生成契约、数据库类型、权限目录和 RPT 定义检查；前后端及共享包类型检查、前端 lint/format/build；共享模型、API 单元、生成物和真实 PostgreSQL 集成测试。
- Session 与 Host 公开 seam 验证资源去重、create-only 入口、无权直达、未登记状态、撤权销毁与异步结果隔离；固定目标域能力矩阵把业务版本与 revision 分开。
- 浏览器通过真实 Session 核对全部授权菜单链接，并从菜单及刷新直达多个域的未实现页；这些场景不发送非 Session 业务请求。无资源权限直达显示无权访问。
- 浏览器覆盖 1280×800、390×844 的明暗主题、侧栏、顶栏、Host、个人资料保存、改密取消/清空及退出；截图只在密码字段为空或不存在时采集。
- 独立 Standards / Spec 审查已修复目录删除规则冲突、DCL 内部告警、用户展示层提取不足；视觉对照恢复原有分组图标。

构建期间 Docker DNS 无法解析 npm registry；以仓库内临时 Compose build host 映射完成验证，不修改正式部署配置。浏览器首轮的动态分组索引与遮罩点击定位问题已修正并通过导航专项重跑。

## 最终结果

最终候选的完整 `make target-e2e` 以退出码 0 结束：共享模型 33、前端单元/组件 50、API 单元 38、生成物 20、PostgreSQL 集成 31、浏览器 4 项全部通过；WFL Node/browser parity 同时通过。Compose 配置有效，API `/healthz` 与 `/readyz` 均正常。

已对照 #379 桌面/390px 顶栏与侧栏截图，以及原用户页窄屏布局，核对最终明暗主题的导航和 Host：颜色、字体、卡片间距、图标、抽屉宽度与移动呈现保持既有风格。截图保留在本地验收目录，不包含已填充密码。

### Standards

独立审查与主会话复核完成，未解决发现 0 项。

### Spec

本票验收范围已完成，未解决发现 0 项；app/user 与其他资源的业务页面、DCL/ACC/配置资料迁移仍未交付，不计入本票成果。

`make check-common` 与 CI 行为检查通过；CI 行为检查复用未变更脚本的 15 项通过证据。隔离 API/Web/PostgreSQL 容器、卷、网络及临时构建配置已显式清理；既有共享服务未动。
