# VOU 动态单据页验收（#411）

- 实现 SHA：`7fb517af87b731c076ac96d812c2a22aa2b39cdd`。
- 范围：36 类普通单据和独立会计期初统一进入 DocumentPage；销售订单、采购订单完成人工录入。其他 30 类人工录入仍明确显示未开放，4 类系统生成单据不提供人工创建。没有生产部署或业务数据迁移。
- 独占环境：Compose project `zerp-issue-411`，PostgreSQL/API/Web 端口 `55442/18088/18089`，数据库 `zerp_target_test`；连接配置由进程环境传递。

## 验收结果

隔离命令前缀为 `sh .scratch/issue-411/run.sh`，设置独占端口、数据库环境和 Compose project。临时脚本及日志仅在本地 `.scratch/issue-411/`，不进入仓库。

| 命令                                                                                | 退出码 | 证据                                                                                                                          |
| ----------------------------------------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------- |
| 隔离环境 `make check e2e`                                                           | 2      | 生成、公共检查、构建、模型 31、API 单元 81、持久化 19、集成 86、迁移 6 项通过；首次浏览器 27 通过、1 失败，聚合命令未记为成功 |
| `pnpm --dir frontend typecheck` 与 `pnpm --dir frontend check`                      | 0      | 最终类型、静态、格式、构建检查，181 项单元和 10 项真实 Vuetify 测试                                                           |
| 隔离环境 `make -o target-test e2e`                                                  | 2      | 重新构建 API/Web；通用浏览器 26 通过、2 失败，未执行后续专项，改为单独完成                                                    |
| 隔离环境 `pnpm --filter @zerp/api e2e -- vou-orders.spec.ts`                        | 0      | 3/3，销售/采购候选、附件、390px、复制重提、审批及无查询权限；日志 `orders-final-3.log`                                        |
| 隔离环境 `pnpm --filter @zerp/api e2e -- vou-orders.spec.ts asset-category.spec.ts` | 1      | 资产类别连接中断重跑通过，订单夹具修复前 3 通过、1 失败                                                                       |
| `pnpm --filter @zerp/api typecheck`                                                 | 0      | 新增原材料夹具通过现有 AUX/BOB Service，类型检查通过                                                                          |
| 隔离环境 `pnpm --filter @zerp/api e2e:wfl`                                          | 0      | 1/1，流程定义、试跑、提交审批及已有实例                                                                                       |
| 隔离环境 `pnpm --filter @zerp/api e2e:vou-catalog`                                  | 0      | 2/2，全部 36 个真实菜单与仅审批权限；日志 `catalog-final-2.log`                                                               |
| 隔离环境 `pnpm --filter @zerp/api e2e:vou-opening`                                  | 0      | 2/2，零期初、另一操作员审批、显式替换非零期初及无查询权限                                                                     |
| `pnpm docs:check`、格式检查与 `git diff --check`                                    | 0      | 权威文档与验收记录通过检查                                                                                                    |

浏览器重跑复用同一候选已通过的模型与后端检查，并由完整前端检查覆盖最后修复，跳过已通过的 `target-test` 前置链。生产后端未修改；E2E 夹具补建真实原材料并通过现有领域服务生效、清理。

首次浏览器失败发现 VOU 的 `idempotencyKey` 必须等于 `submissionId`，运行时已统一生成，公开提交回归补充相等断言。此前完整前端检查还发现旧注册结构断言，已更新为新的两字段单据定义契约。第二轮浏览器暴露复制数量断言未使用服务端标准化快照，以及资产类别一次 socket hang up；资产类别独立重跑通过。订单测试随后改用经营主体候选的编码定位，匹配候选展示简称的契约。销售测试还正确拦截旧来源配方使用定制品自身作原料；已补建 RAW_MATERIAL 夹具，验证真实当前原料重新解析。目录专项使用仅审批账号，新建按钮断言已收紧为不可见；创建能力由有权订单菜单 E2E 独立覆盖。

## 公开入口与行为证据

- definition → Registry/ResourceHost → DocumentPage 为唯一入口。定义只包含 `kind` 与 `vouType`；运行时持有查询、临时编辑、提交身份、审批、附件、刷新和未知结果核实。
- 17 项单据公共组件回归覆盖录入、客户默认备注与内部提醒、原材料自身配方、复制手工配方时重新采用当前原料、数量与加价保留、未知写入锁定、取消不刷新、成功只刷新一次、刷新失败不重放、Session/资源切换以及迟到请求隔离。
- 清空产品、移除商品行会使在途请求失效并释放等待状态；复制配方首次挂载前装配完成，原料重新解析保留原数量快照。
- 真实菜单覆盖销售/采购订单候选选择、商品行、附件暂存、390px 提交、不可变详情回读、复制后重新提交和原单不变。已有真实 HTTP/PostgreSQL 测试覆盖权限、信用超额、审批入账、来源/下游 blocker、附件与期初事务。
- 旧 VOU 包装页面、VM、列表壳及专属私有状态测试删除，保留通用区块复用；期初载荷仍独立。

## 独立审查

- Standards：候选清空/移除后的 pending 泄漏已修复并补公共入口回归；复核无未解决项。
- Spec：历史未采用原因提示、克隆配方挂载及当前原料重新解析已修复；最终复核无未解决项。

## 资源回收

隔离环境 `make target-down` 与删除本任务 API/Web 镜像退出 0。独占容器、网络、附件卷及两个镜像已删除；回读三个独占端口无监听，专项脚本无残留进程。共享服务保持运行。
