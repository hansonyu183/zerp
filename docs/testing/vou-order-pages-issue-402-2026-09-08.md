# #402 销售与采购单据列表验收

## 范围与结果

本地候选完成销售、采购真实 Registry 接入，独立单据字段契约、摘要查询、完整只读详情、审批及权限隔离。领域依据见 [VOU](../domains/vou.md)，页面编排见[销售订单](../use-cases/vou/sale-order.md)与[采购订单](../use-cases/vou/purchase-order.md)。本记录不表示合并或发布。

| 能力                               | 销售订单                      | 采购订单                      |
| ---------------------------------- | ----------------------------- | ----------------------------- |
| 菜单、摘要检索、打开               | 已验证                        | 已验证                        |
| 期间、单号、提交日期、状态、相对方 | 已验证                        | 已验证                        |
| 完整不可变详情                     | 已验证                        | 已验证                        |
| 驳回、恢复审核、批准、反批准       | HTTP 已验证；浏览器批准已验证 | HTTP 已验证；浏览器批准已验证 |
| 390px 查询与操作                   | 已验证                        | 已验证                        |
| 专用编辑器                         | 尚未实施，页面明确提示        | 尚未实施，页面明确提示        |

查询不会加载整单 payload；经办人缺失为 null；相对方名称读取精确历史版本。订单金额读取产品行定点事实汇总，未使用不承载合计的表头值。没有数据库 schema 变更或数据迁移。

## 验证证据

- `pnpm --filter @zerp/api generate:artifacts`：完成；重复生成摘要一致。
- API、API client、前端 typecheck：通过；前端 lint、format、build 和根 format/docs 检查：通过。
- API 单元测试 80 项、生成脚本测试 19 项、共享模型测试 31 项：通过。
- 前端全量单元测试 205 项及 Vuetify 组件测试 10 项通过；之后新增的精确审批原因核实回归与 VOU VM 共 6 项通过。
- 直接使用授权的 `zerp` 数据库，分项运行 `vou-core.int.test.ts`、`vou-control.int.test.ts`、`vou-orders.int.test.ts`，共 7 项通过。包含真实会计控制、审批并发、提交回滚、附件、引用与订单 HTTP 筛选/分页/详情/审批。
- `vou-orders.spec.ts` 两项真实浏览器测试通过：从菜单进入销售/采购，在 390px 检索、取消审批及批准；仅审批资源不发送 query 或引用请求。API/Web 临时进程显式关闭，订单专用测试数据随现有事务夹具回滚。全局 E2E 脚本已装配本票所需待批准订单与精确权限账号。
- Standards 和 Spec 独立审查完成；确认状态迁入 VM、复用共享 Approval 文案后，复核未发现阻断问题。

未调用会隐式清库的聚合 Make 命令，未执行部署或更改 CI。浏览器缩放时出现 ResizeObserver 通知循环诊断，但两条实际用户流程及宽度断言均通过；生产构建保留既有大 chunk 提示。
