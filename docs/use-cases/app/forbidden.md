# 无权访问页面用例

## 范围

- 路由：`/forbidden`，由 `app/forbidden` 登记。
- 权限计算与导航判定以 [APP 最终权限计算](../../domains/app.md#4-最终权限计算) 为准。

## `APP-FORBIDDEN-01` 拒绝无权限页面访问

1. 导航守卫发现当前会话不具备页面所需权限时，跳转到本页。
2. 页面说明当前账号没有访问权限，不泄露被拒绝页面的数据或动作。

## 验收

1. 本页不尝试通过前端状态恢复被拒绝页面。
2. 权限变化后由后续导航和服务端校验重新决定可访问性。
