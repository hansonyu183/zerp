# 页面不存在用例

## 范围

- 路由：`/:pathMatch(.*)*`，由 `app/not-found` 登记。
- 这是未知地址的界面反馈，不定义业务对象或接口行为；可用导航入口受 [APP 菜单模板](../../domains/app.md#39-菜单模板) 约束。

## `APP-NOT-FOUND-01` 处理未知地址

1. 路由表未匹配地址时显示 404 说明。
2. 用户可返回工作台，再从已授权的导航入口继续操作。

## 验收

1. 未知地址不得回落到不相干的业务页面。
2. 本页不发起业务写入请求。
