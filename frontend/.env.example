# 本地开发与容器部署统一使用同源代理。
VITE_API_BASE_URL=/api/

# 以下变量仅供 Playwright 本地/CI 进程读取，不会进入前端构建。
E2E_API_BASE_URL=https://api-test.example.com/
E2E_USERNAME=
E2E_PASSWORD=
# Playwright 只连接可销毁的隔离后端。每个 worker 通过真实 API 创建
# 独立操作员、复核员、基础资料、库存和会话，不接受共享外部资料。
